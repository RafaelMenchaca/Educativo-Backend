# Anexos (referencia histórica)

Este archivo contenía una propuesta de prompt y estructura que podía confundirse con el contrato ejecutable.

El contrato documental vigente de anexos está en [`../AI_GENERATION_CONTRACTS.md`](../AI_GENERATION_CONTRACTS.md), sección “Anexos”. El schema de persistencia está en [`../DATABASE_SCHEMA.md`](../DATABASE_SCHEMA.md). El prompt ejecutable permanece en `src/services/anexos.service.js` y no fue modificado.

No copiar prompts completos a este archivo ni tratar propuestas históricas como instrucciones de producción.
